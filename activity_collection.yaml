CZBIActivityCollection:
	type: nested
	properties:
		specimen_name:
			type: fulltext+keyword
			label: Specimen label
			required: true
		taxon:
			type: int
			hint: ID from some ontology (for now)
		description:
			type: fulltext
			label: Description
			required: true
